<odoo>
    <data>
        <!--new paper format-->
        <record model="report.paperformat" id="rfq_paperformat">
            <field name="name">RFQ Format</field>
            <field name="default" eval="True" />
            <field name="format">A4</field>
            <field name="page_height">0</field>
            <field name="page_width">0</field>
            <field name="orientation">Portrait</field>
            <field name="margin_top">95</field>
            <field name="margin_bottom">40</field>
            <field name="margin_left">5</field>
            <field name="margin_right">5</field>
            <field name="header_line" eval="False"/>
            <field name="header_spacing">90</field>
            <field name="dpi">90</field>
        </record>
        <!--pdf report-->
        <record id="sale.action_report_saleorder" model="ir.actions.report">
            <field name="paperformat_id" ref="final_exam_hashmicro.rfq_paperformat"/>
        </record>

        <template id="report_saleorder_inherit" inherit_id="sale.report_saleorder_document">
            <xpath expr="//t" position="replace">
                <t t-call="web.basic_layout">
                    <div class="header">
                        <div style="border-bottom: 2px solid black; padding-bottom: 5px;">
                            <table width="100%" style="color:black;">
                                <tr>
                                    <th width="50%" style="padding-left: 5px;">
                                        <img t-att-src="image_data_uri(doc.company_id.logo)" style="max-height: 100px;"/>
                                    </th>
                                    <th width="50%" class="text-right" style="padding-right: 5px; vertical-align: bottom;">
                                        <span style="font-size: 30px;">SURAT PENAWARAN</span><br/>
                                        <span style="font-size: 26px;" t-if="doc.state not in ['draft','sent']">Order # </span>
                                        <span style="font-size: 26px;" t-if="doc.state in ['draft','sent']">Quotation # </span>
                                        <span style="font-size: 26px;" t-field="doc.name"/>
                                    </th>
                                </tr>
                            </table>
                        </div>

                        <div style="padding-top: 5px;">
                            <table width="100%" style="color:black;">
                                <tr>
                                    <th width="60%" style="padding-left: 5px;" rowspan="6">
                                        <span t-field="doc.company_id.name"/>

                                        <!--address-->
                                        <!--    street-->
                                        <t t-if="doc.company_id.street">
                                            <br/>
                                            <t t-if="doc.company_id.street2">
                                                <span t-field="doc.company_id.street"/>,
                                                <span t-field="doc.company_id.street2"/>
                                            </t>
                                            <t t-else="">
                                                <span t-field="doc.company_id.street"/>,
                                            </t>
                                        </t>

                                        <!--    city-->
                                        <t t-if="doc.company_id.city">
                                            <br/>
                                            <span t-field="doc.company_id.city"/>,
                                        </t>

                                        <!--    state-->
                                        <t t-if="doc.company_id.state_id">
                                            <span t-field="doc.company_id.state_id.name"/>,
                                        </t>

                                        <!--    country-->
                                        <t t-if="doc.company_id.country_id">
                                            <br/>
                                            <span t-field="doc.company_id.country_id.name"/>
                                        </t>

                                        <!--    zip-->
                                        <t t-if="doc.company_id.zip">
                                            - <span t-field="doc.company_id.zip"/>
                                        </t>

                                        <!--    mobile-->
                                        <t t-if="doc.company_id.mobile">
                                            <br/>
                                            <span t-field="doc.company_id.mobile"/>,
                                        </t>

                                        <!--    phone-->
                                        <t t-if="doc.company_id.phone">
                                            <br/>
                                            <img src='/final_exam_hashmicro/static/src/img/phone.png' style="max-height: 10px;"/>
                                            <span t-field="doc.company_id.phone"/>
                                        </t>

                                        <!--    email-->
                                        <t t-if="doc.company_id.email">
                                            <br/>
                                            <img src='/oke_print/static/src/img/mail.png' style="max-height: 10px;"/>
                                            <span t-field="doc.company_id.email"/>
                                        </t>
                                    </th>
                                    <th width="40%">
                                        Quotation Date:
                                    </th>
                                </tr>
                                <tr>
                                    <td>
                                        <span t-field="doc.date_order"/>
                                    </td>
                                </tr>
                                <tr>
                                    <th>
                                        Quotation Expired:
                                    </th>
                                </tr>
                                <tr>
                                    <td rowspan="3">
                                        <span t-field="doc.validity_date"/>
                                    </td>
                                </tr>
                            </table>

                            <br/>
                            <table width="100%" style="color:black;">
                                <tr>
                                    <th width="60%" style="padding-left: 5px;">
                                        To:
                                    </th>
                                    <th width="40%">
                                        Sales Person:
                                    </th>
                                </tr>
                                <tr>
                                    <td style="padding-left: 5px;">
                                        <span t-field="doc.partner_id.display_name"/><br/>
                                        <t t-if="doc.partner_id.street">
                                            <span t-field="doc.partner_id.street"/>
                                        </t>
                                        <t t-if="doc.partner_id.street2">
                                            <span t-field="doc.partner_id.street2"/>
                                        </t>
                                        <t t-if="doc.partner_id.city">
                                            <span t-field="doc.partner_id.city"/>
                                        </t>
                                        <t t-if="doc.partner_id.state_id">
                                            <span t-field="doc.partner_id.state_id.name"/>
                                        </t>
                                        <t t-if="doc.partner_id.zip">
                                            - <span t-field="doc.partner_id.zip"/>
                                        </t>
                                    </td>
                                    <td>
                                        <span t-field="doc.user_id.name"/>
                                    </td>
                                </tr>
                            </table>
                        </div>
                    </div>

                    <div class="page">
                        <div class="oe_structure"/>

                        <!-- Is there a discount on at least one line? -->
                        <t t-set="display_discount" t-value="any(l.discount for l in doc.order_line)"/>

                        <table class="table table-sm o_main_table">
                            <!-- In case we want to repeat the header, remove "display: table-row-group" -->
                            <thead style="display: table-row-group">
                                <tr>
                                    <th name="th_description" class="text-left">Description</th>
                                    <th name="th_quantity" class="text-right">Quantity</th>
                                    <th name="th_priceunit" class="text-right">Unit Price</th>
                                    <th name="th_discount" t-if="display_discount" class="text-right" groups="product.group_discount_per_so_line">
                                        <span>Disc.%</span>
                                    </th>
                                    <th name="th_subtotal" class="text-right">
                                        <span groups="account.group_show_line_subtotals_tax_excluded">Amount</span>
                                        <span groups="account.group_show_line_subtotals_tax_included">Total Price</span>
                                    </th>
                                </tr>
                            </thead>
                            <tbody class="sale_tbody">

                                <t t-set="current_subtotal" t-value="0"/>

                                <t t-foreach="doc.order_line" t-as="line">

                                    <t t-set="current_subtotal" t-value="current_subtotal + line.price_subtotal"
                                       groups="account.group_show_line_subtotals_tax_excluded"/>
                                    <t t-set="current_subtotal" t-value="current_subtotal + line.price_total"
                                       groups="account.group_show_line_subtotals_tax_included"/>

                                    <tr t-att-class="'bg-200 font-weight-bold o_line_section' if line.display_type == 'line_section' else 'font-italic o_line_note' if line.display_type == 'line_note' else ''">
                                        <t t-if="not line.display_type">
                                            <td name="td_name"><span t-field="line.name"/></td>
                                            <td name="td_quantity" class="text-right">
                                                <span t-field="line.product_uom_qty"/>
                                                <span t-field="line.product_uom"/>
                                            </td>
                                            <td name="td_priceunit" class="text-right">
                                                <span t-field="line.price_unit"/>
                                            </td>
                                            <td t-if="display_discount" class="text-right" groups="product.group_discount_per_so_line">
                                                <span t-field="line.discount"/>
                                            </td>
                                            <td name="td_subtotal" class="text-right o_price_total">
                                                <span t-field="line.price_subtotal" groups="account.group_show_line_subtotals_tax_excluded"/>
                                                <span t-field="line.price_total" groups="account.group_show_line_subtotals_tax_included"/>
                                            </td>
                                        </t>
                                        <t t-if="line.display_type == 'line_section'">
                                            <td name="td_section_line" colspan="99">
                                                <span t-field="line.name"/>
                                            </td>
                                            <t t-set="current_section" t-value="line"/>
                                            <t t-set="current_subtotal" t-value="0"/>
                                        </t>
                                        <t t-if="line.display_type == 'line_note'">
                                            <td name="td_note_line" colspan="99">
                                                <span t-field="line.name"/>
                                            </td>
                                        </t>
                                    </tr>

                                    <t t-if="current_subtotal > 0">
                                        <t t-if="current_section and (line_last or doc.order_line[line_index+1].display_type == 'line_section')">
                                            <tr class="is-subtotal text-right">
                                                <td name="td_section_subtotal" colspan="99">
                                                    <strong class="mr16">Subtotal</strong> &#160; &#160;
                                                    <span
                                                        t-esc="current_subtotal"
                                                        t-options='{"widget": "monetary", "display_currency": doc.pricelist_id.currency_id}'
                                                    />
                                                </td>
                                            </tr>
                                        </t>
                                    </t>
                                </t>
                            </tbody>
                        </table>

                        <div class="clearfix" name="so_total_summary">
                            <div id="total" class="row" name="total">
                                <div t-attf-class="#{'col-6' if report_type != 'html' else 'col-sm-7 col-md-6'} ml-auto">
                                    <table class="table table-sm">
                                        <!-- Tax totals -->
<!--                                        <t t-set="tax_totals" t-value="json.loads(doc.tax_totals_json)"/>-->
<!--                                        <t t-call="account.document_tax_totals"/>-->
                                    </table>
                                </div>
                            </div>
                        </div>

                        <div t-if="doc.signature" class="mt32 ml64 mr4" name="signature">
                            <div class="offset-8">
                                <strong>Signature</strong>
                            </div>
                            <div class="offset-8">
                                <img t-att-src="image_data_uri(doc.signature)" style="max-height: 4cm; max-width: 8cm;"/>
                            </div>
                            <div class="offset-8 text-center">
                                <p t-field="doc.signed_by"/>
                            </div>
                        </div>
                    </div>

                    <div class="footer">
                        <div style="padding-bottom: 5px;">
                            <table width="100%" style="color:black;">
                                <tr>
                                    <th width="60%" style="padding-left: 5px; border: 1px solid lightgrey;" rowspan="4">
                                        <t t-if="doc.quotation_printout_note">
                                            <span t-field="doc.quotation_printout_note"/>
                                        </t>
                                    </th>
                                    <th width="40%" class="text-center">
                                        <span t-esc="context_timestamp(datetime.datetime.now()).strftime('%d %B %Y %H:%M:%S')"/>
                                    </th>
                                </tr>
                                <tr>
                                    <th class="text-center">
                                        <t t-if="doc.responsible_signature">
                                            <img t-att-src="image_data_uri(doc.responsible_signature)" style="max-height: 75px;"/>
                                        </t>
                                    </th>
                                </tr>
                                <tr>
                                    <th class="text-center">
                                        <span t-field="doc.responsible_name"/>
                                    </th>
                                </tr>
                                <tr>
                                    <th class="text-center">
                                        <span t-field="doc.responsible_position"/>
                                    </th>
                                </tr>
                            </table>
                        </div>

                        <div class="text-center" style="border-top: 2px solid black; padding-top: 5px;">
                            Page: <span class="page"/> / <span class="topage"/>
                        </div>
                    </div>
                </t>
            </xpath>
        </template>

        <!--customer preview/ portal-->
        <template id="sale_order_portal_inherit" inherit_id="sale.sale_order_portal_content">
            <xpath expr="//section[@id='details']" position="replace">
                <section id="details" style="page-break-inside: auto;" class="mt32">
                <h3 id="details">Pricing</h3>

                <t t-set="display_discount" t-value="True in [line.discount > 0 for line in sale_order.order_line]"/>

                <table t-att-data-order-id="sale_order.id" t-att-data-token="sale_order.access_token" class="table table-sm" id="sales_order_table">
                    <thead class="bg-100">
                        <tr>
                            <th class="text-left">Products</th>
                            <th class="text-right">Quantity</th>
                            <th t-attf-class="text-right {{ 'd-none d-sm-table-cell' if report_type == 'html' else '' }}">Unit Price</th>
                            <th t-if="display_discount" t-attf-class="text-right {{ 'd-none d-sm-table-cell' if report_type == 'html' else '' }}">
                                <span>Disc.%</span>
                            </th>
                            <th t-attf-class="text-right {{ 'd-none d-md-table-cell' if report_type == 'html' else '' }}">
                                <span>Taxes</span>
                            </th>
                            <th class="text-right" >
                                <span groups="account.group_show_line_subtotals_tax_excluded">Amount</span>
                                <span groups="account.group_show_line_subtotals_tax_included">Total Price</span>
                            </th>
                        </tr>
                    </thead>
                    <tbody class="sale_tbody">

                        <t t-set="current_subtotal" t-value="0"/>

                        <t t-foreach="sale_order.order_line" t-as="line">

                            <t t-set="current_subtotal" t-value="current_subtotal + line.price_subtotal" groups="account.group_show_line_subtotals_tax_excluded"/>
                            <t t-set="current_subtotal" t-value="current_subtotal + line.price_total" groups="account.group_show_line_subtotals_tax_included"/>

                            <tr t-att-class="'bg-200 font-weight-bold o_line_section' if line.display_type == 'line_section' else 'font-italic o_line_note' if line.display_type == 'line_note' else ''">
                                <t t-if="not line.display_type">
                                    <td id="product_name"><span t-field="line.name"/></td>
                                    <td class="text-right">
                                        <div id="quote_qty">
                                            <span t-field="line.product_uom_qty"/>
                                            <span t-field="line.product_uom"/>
                                        </div>
                                    </td>
                                    <td t-attf-class="text-right {{ 'd-none d-sm-table-cell' if report_type == 'html' else '' }}">
                                        <div
                                            t-if="line.discount &gt;= 0"
                                            t-field="line.price_unit"
                                            t-att-style="line.discount and 'text-decoration: line-through' or None"
                                            t-att-class="(line.discount and 'text-danger' or '') + ' text-right'"
                                        />
                                        <div t-if="line.discount">
                                            <t t-esc="(1-line.discount / 100.0) * line.price_unit" t-options='{"widget": "float", "decimal_precision": "Product Price"}'/>
                                        </div>
                                    </td>
                                    <td t-if="display_discount" t-attf-class="text-right {{ 'd-none d-sm-table-cell' if report_type == 'html' else '' }}">
                                        <strong t-if="line.discount &gt; 0" class="text-info">
                                            <t t-esc="((line.discount % 1) and '%s' or '%d') % line.discount"/>%
                                        </strong>
                                    </td>
                                    <td t-attf-class="text-right {{ 'd-none d-md-table-cell' if report_type == 'html' else '' }}">
                                        <span t-esc="', '.join(map(lambda x: (x.description or x.name), line.tax_id))"/>
                                    </td>
                                    <td class="text-right">
                                        <span class="oe_order_line_price_subtotal" t-field="line.price_subtotal" groups="account.group_show_line_subtotals_tax_excluded"/>
                                        <span class="oe_order_line_price_total" t-field="line.price_total" groups="account.group_show_line_subtotals_tax_included"/>
                                    </td>
                                </t>
                                <t t-if="line.display_type == 'line_section'">
                                    <td colspan="99">
                                        <span t-field="line.name"/>
                                    </td>
                                    <t t-set="current_section" t-value="line"/>
                                    <t t-set="current_subtotal" t-value="0"/>
                                </t>
                                <t t-if="line.display_type == 'line_note'">
                                    <td colspan="99">
                                        <span t-field="line.name"/>
                                    </td>
                                </t>
                            </tr>

                            <t t-if="current_subtotal > 0">
                                <t t-if="current_section and (line_last or sale_order.order_line[line_index+1].display_type == 'line_section')">
                                    <tr class="is-subtotal text-right">
                                        <td colspan="99">
                                            <strong class="mr16">Subtotal</strong>
                                            <span
                                                t-esc="current_subtotal"
                                                t-options='{"widget": "monetary", "display_currency": sale_order.pricelist_id.currency_id}'
                                            />
                                        </td>
                                    </tr>
                                </t>
                            </t>
                        </t>
                    </tbody>
                </table>

                <div id="total" class="row" name="total" style="page-break-inside: avoid;">
                    <div t-attf-class="#{'col-6' if report_type != 'html' else 'col-sm-7 col-md-6'} ml-auto">
                        <t t-call="sale.sale_order_portal_content_totals_table"/>
                    </div>
                </div>
            </section>
            </xpath>
        </template>

        <!--new template-->
        <template id="document_tax_totals">
            <!--
                Generic template to display tax totals in pdf reports.
                Used by invoices, SO and Pdoc.

                ARGUMENTS:
                - tax_totals: dict in the form generated by account.move's _get_tax_totals.
            -->
            <t t-foreach="tax_totals['subtotals']" t-as="subtotal">
                <tr class="border-black o_subtotal">
                    <td style="text-transform: uppercase;"><strong t-esc="subtotal['name']"/></td>

                    <td class="text-right">
                        <span
                            t-att-class="oe_subtotal_footer_separator"
                            t-esc="subtotal['formatted_amount']"
                        />
                    </td>
                </tr>

                <t t-set="subtotal_to_show" t-value="subtotal['name']"/>
                <t t-call="oke_print.tax_groups_totals"/>
            </t>

            <!--Total amount with all taxes-->
            <tr class="border-black o_total">
                <td><strong>TOTAL</strong></td>
                <td class="text-right">
                    <span t-esc="tax_totals['formatted_amount_total']"/>
                </td>
            </tr>
        </template>

        <template id="tax_groups_totals">
            <!--
                Generic template to display a list of tax groups with the related amounts.

                ARGUMENTS:
                - tax_totals: dict in the form generated by account.move's _get_tax_totals.
                - subtotal_to_show: The subtotal we need to render the groups from
            -->
            <t t-foreach="tax_totals['groups_by_subtotal'][subtotal_to_show]" t-as="amount_by_group">
                <tr>
                    <t t-if="len(tax_totals['groups_by_subtotal'][subtotal_to_show]) > 1 or (tax_totals['amount_untaxed'] != amount_by_group['tax_group_base_amount'])">
                        <td style="text-transform: uppercase;">
                            <span t-esc="amount_by_group['tax_group_name']"/>
                            <span class="text-nowrap"> ON
                                <t t-esc="amount_by_group['formatted_tax_group_base_amount']"/>
                            </span>
                        </td>
                        <td class="text-right o_price_total">
                            <span class="text-nowrap" t-esc="amount_by_group['formatted_tax_group_amount']"/>
                        </td>
                    </t>
                    <t t-else="">
                        <td style="text-transform: uppercase;">
                            <span class="text-nowrap" t-esc="amount_by_group['tax_group_name']"/>
                        </td>
                        <td class="text-right o_price_total">
                            <span class="text-nowrap" t-esc="amount_by_group['formatted_tax_group_amount']" />
                        </td>
                    </t>
                </tr>
            </t>
        </template>
    </data>
</odoo>
